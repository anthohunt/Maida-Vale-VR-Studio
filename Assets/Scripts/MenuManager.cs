using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Oculus.Interaction;
using System.Threading;

//Activate/deactivate instrument selection and FPS display
public class MenuManager : MonoBehaviour
{
  //  public GameObject instrumentMenu; // Reference to the instrument menu game object
  //  public OVRPassthroughLayer fullPassthrough;

    // deactivated because I can't find a way to get the hand input deactivated
    //void Update()
    //{
    //    if (OVRInput.GetDown(OVRInput.Button.Start))
    //    {
    //        instrumentMenu.SetActive(instrumentMenu.activeInHierarchy ? false : true);
    //    }
    //}
}
